# Database Configuration (external PostgreSQL)
# Format: postgresql://user:password@host:port/database
# For Docker: use host.docker.internal instead of localhost to access external DB
# For local development: use localhost
DATABASE_URL=postgresql://user:password@localhost:5432/role_distribution

# CORS Origins (JSON array format required by pydantic-settings)
# For Docker: nginx handles CORS, but backend still needs origins configured
# For local development: use http://localhost:5173
CORS_ORIGINS=["http://localhost:5173"]
